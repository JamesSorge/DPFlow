# DPFlow
This is a reimplementation of the DPFlow paper from https://arxiv.org/abs/2503.14880

We studied and reimplemented the bidirectional encoder, which is the core part of the model that takes a raw image as input and outputs a feature pyramid.
